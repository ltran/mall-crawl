<%= form_for(@crawl) do |f| %>
  <% if @crawl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crawl.errors.count, "error") %> prohibited this crawl from being saved:</h2>

      <ul>
      <% @crawl.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :centre_id %><br>
    <%= f.collection_select(:centre_id, Centre.all, :id, :centre_id) %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
